# Tasks: Protocol-Aware Asymmetric Decay and Stateful Motif Pruning in CTDGs

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done

## Phase 0 — Foundations

- [x] 0.1 Stand up base infra: Apache Flink cluster, Redis instance, Neo4j instance (dev environment). *(docker-compose.yml written as the target stack; local dev currently redirected to a Postgres instance instead -- see README.md "Local dev database". Bring up the compose stack when Flink/Redis/Neo4j-backed phases need it.)*
- [x] 0.2 Define edge schema (`src`, `dst`, `protocol`, `t_e`, `w_0`, `w(e,t)`) as a shared data contract across Flink, PyG, Redis, Neo4j. *(config/schema/edge.schema.json + src/t_gnn/schema.py)*
- [x] 0.3 Define initial protocol set and placeholder decay constants (RDP, SMB, Kerberos, DNS) for early testing. *(config/protocols.yaml + src/t_gnn/protocol_registry.py)*
- [x] 0.4 Acquire and stage the LANL Comprehensive Cybersecurity dataset for offline replay/calibration (specs.md FR5.4). *(src/t_gnn/data/stage_lanl.py staging pipeline + data/lanl/README.md acquisition instructions; real dataset download is an operational step, not vendored in-repo)*
- [x] 0.5 Build a Sysmon/Windows Event Log ingestion adapter mapping Sysmon event IDs to typed edges (Authentication, File Transfer, Remote Code Execution) per specs.md FR5.2/FR5.3. *(src/t_gnn/ingestion/sysmon_adapter.py)*

## Phase 1 — Protocol-Aware Asymmetric Time-Decay (FR1)

- [x] 1.1 Implement Protocol Decay Registry (broadcast-state config: `protocol → λ_p`) in Flink. *(src/t_gnn/protocol_registry.py `ProtocolDecayRegistry` -- staged as a framework-agnostic loader per CLAUDE.md's Postgres-redirect philosophy; becomes the payload of real Flink broadcast state once a phase needs that cluster's live semantics.)*
- [x] 1.2 Implement hot-reload mechanism for `λ_p` updates without job redeploy. *(`ProtocolDecayRegistry.reload()`, already present from Phase 0 -- re-reads config/protocols.yaml from disk without restarting the process.)*
- [x] 1.3 Implement streaming computation of `w(e,t) = w_0 · e^(-λ_p (t - t_e))` per active edge. *(src/t_gnn/decay.py `compute_weight()` + `DecayEngine`.)*
- [x] 1.4 Implement rolling baseline distribution model per `(entity, protocol)` (keyed Flink state, e.g., EWMA mean/variance). *(src/t_gnn/baseline.py `EWMABaseline` + `BaselineStore`, keyed in-memory dict standing in for Flink keyed state; entity = edge.src.)*
- [x] 1.5 Implement deviation-signal computation (e.g., z-score of current aggregated weight vs. baseline). *(src/t_gnn/baseline.py `BaselineStore.observe()` returns a `DeviationSignal` with a z-score computed against the pre-update baseline; src/t_gnn/streaming.py `DecayStreamProcessor` wires decay + baseline into one per-edge step.)*
- [x] 1.6 Unit tests: decay curve correctness per protocol; verify RDP decays faster than SMB under identical `t - t_e`. *(tests/test_decay.py, tests/test_baseline.py, tests/test_streaming.py.)*
- [x] 1.7 Calibration pass: derive/tune initial `λ_p` values using the LANL dataset replay (task 0.4) as the primary benchmark, supplemented by production Sysmon telemetry once available (or expert defaults if neither is available). *(src/t_gnn/data/calibrate_decay.py -- derives suggested λ_p per protocol from median same-entity inter-arrival gaps in staged LANL edges, reporting `sufficient_data`/falling back to the current expert default when a protocol has too few samples; only the tiny synthetic fixture is vendored, so real calibration awaits the full LANL dataset per task 0.4.)*

## Phase 2 — Dynamic Graph Pruning (FR2)

- [x] 2.1 Design and implement the Active Graph Store as a mutable structure compatible with PyTorch Geometric (dynamic insert/remove of edges). *(src/t_gnn/graph_store.py `ActiveGraphStore` -- hash-map-keyed edge store + per-node adjacency sets per design.md's `TemporalEdgeStore`; `to_pyg_edge_index()` materializes a live torch.LongTensor edge_index on demand.)*
- [x] 2.2 Implement Pruning Watcher background process: continuous scan/evaluation of `w(e,t) < ε`. *(src/t_gnn/pruning.py `PruningWatcher.run_once()`/`start()`/`stop()` -- daemon-thread loop calling `run_once()` on `poll_interval`.)*
- [x] 2.3 Implement memory-pressure feedback loop to compute dynamic `ε` (rising under pressure, relaxing when headroom available). *(src/t_gnn/pruning.py `EpsilonController` -- interpolates between `epsilon_min`/`epsilon_max` using the max of real system-memory pressure (`psutil`, via `default_memory_probe()`) and graph-size pressure vs. a configurable `max_edges` ceiling.)*
- [x] 2.4 Implement edge serialization + write path to Neo4j cold storage on prune. *(src/t_gnn/cold_storage.py `Neo4jColdStorageWriter` -- writes `(Entity)-[:PRUNED_EDGE]->(Entity)` via the real `neo4j` driver against the now-running docker-compose Neo4j instance; write happens before removal from the Active Graph Store per FR2.4, with a failed write leaving the edge active for retry rather than dropping it. Buffering so a slow write never blocks pruning is deliberately deferred to task 6.4.)*
- [x] 2.5 Implement "pruned edge" event publication (internal bus/topic) for downstream consumers (Motif Engine). *(src/t_gnn/pruning.py `PruneEventBus`/`PrunedEdgeEvent` -- in-process pub/sub; Phase 3's Motif Engine is the first real subscriber and doesn't exist yet, so no Redis/Kafka-backed bus is justified for this hop today.)*
- [x] 2.6 Ensure pruning runs asynchronously and does not block T-GNN inference reads (FR2.5). *(`PruningWatcher` runs on its own daemon thread; `ActiveGraphStore`'s lock is held only for the brief dict/set mutation inside `remove()`, never across the cold-storage write's network I/O, so reads are never blocked on slow writes.)*
- [x] 2.7 Load test: verify Active Graph Store size stays bounded under sustained high-volume synthetic ingest. *(tests/test_load.py `test_active_graph_store_stays_bounded_under_sustained_ingest` -- proxy-scale smoke test, not literal enterprise volume.)*
- [x] 2.8 Latency test: verify T-GNN inference remains sub-millisecond (NFR1) with pruning active under load. *(tests/test_load.py `test_reads_stay_fast_while_pruning_runs_concurrently` -- `store.get()` stands in for a T-GNN inference read since no real inference engine exists yet (Phase 5); proxy latency check, not a production benchmark.)*

## Phase 3 — Stateful Motif Caching (FR3)

- [x] 3.1 Define initial motif definition schema (ordered/typed edge-pattern sequence + completion condition + time bound). *(config/schema/motif.schema.json + src/t_gnn/motifs.py `MotifStep`/`MotifDefinition` -- same schema/Python-representation split as the edge contract (0.2). A step is a structural pattern (edge_type/protocol/src_type/dst_type) plus a `key_field`/`key_resolver` pair describing how it chains to the entity bound by the previous step; `window_seconds` is the completion time bound.)*
- [x] 3.2 Implement the motif engine generically (any operator-defined pattern), then load two seed motifs for initial calibration/testing: `User → Service Account → Admin Share`, and the originating scenario's timed two-hop pattern `Machine A → Machine B (auth), then Machine B's admin account → Machine C (auth/RCE)` within a bounded window (e.g., ~4 hours). *(src/t_gnn/motif_engine.py `MotifEngine`, generic over any `MotifDefinition`; config/motifs.yaml's `admin_share_escalation` and `lateral_pivot` entries are the two FR3.1 seed motifs, loaded through `MotifRegistry` (3.1).)*
- [x] 3.3 Implement Redis-backed motif-state data structure (per candidate entity chain: stage, last_edge_ts, matched_edges). *(src/t_gnn/motif_engine.py `RedisMotifStateStore` -- a Redis hash per `(motif_name, chain_key)` holding `stage`/`started_at`/`last_edge_ts`/`matched_edges`, plus a reverse-index Redis set per edge id (used by 3.6) -- against the real, now-running docker-compose Redis instance, same "genuinely wired up" treatment 2.4 gave Neo4j. `InMemoryMotifStateStore` is the unit-test fake, mirroring `InMemoryColdStorageWriter`.)*
- [x] 3.4 Implement delta-update algorithm: on new edge ingest, advance matching motif state(s) without full-graph traversal. *(src/t_gnn/motif_engine.py `MotifEngine.on_edge()` -- per motif definition, a candidate chain's Redis key is computed directly from the incoming edge's endpoint via the step's `key_resolver` (motifs.py), so lookup is O(1)/direct-key, never a scan; out-of-order or window-exceeding candidates are rejected/dropped rather than advanced.)*
- [x] 3.5 Implement motif-completion alert emission. *(src/t_gnn/motif_engine.py `MotifCompletionEvent`/`MotifAlertBus` -- published when a chain reaches a definition's final stage; distinct signal from the FR1.5 deviation signal per FR3.4.)*
- [x] 3.6 Implement motif-reset-on-prune: subscribe to "pruned edge" events (2.5) and reset motif state if a matched edge is evicted. *(src/t_gnn/motif_engine.py `MotifEngine.on_prune()`, auto-subscribed to a `PruneEventBus` (2.5) when one is passed to the constructor; looks up affected states via the state store's reverse edge-id index and deletes them.)*
- [x] 3.7 Implement TTL-based motif-state expiry as a safety net independent of explicit prune events. *(`RedisMotifStateStore.set()` applies `EXPIRE` for the motif's `window_seconds` on every state write (and its reverse-index entries); `InMemoryMotifStateStore` mirrors the same expiry against an injectable clock for deterministic unit tests.)*
- [x] 3.8 Unit/integration tests: motif completes correctly on matching sequence; motif resets correctly when a contributing edge is pruned before completion. *(tests/test_motifs.py, tests/test_motif_engine.py -- delta-update/completion/reset/TTL unit tests against `InMemoryMotifStateStore`, an end-to-end run of the real seed motifs from `MotifRegistry`, and live-Redis integration tests skipped (not failed) if `docker compose up -d` hasn't been run, mirroring test_cold_storage.py's pattern.)*
- [x] 3.9 Design motif definition extensibility mechanism (config-driven vs. code-driven) per FR3.5. *(Documented in src/t_gnn/motifs.py's module docstring: new motifs expressible as typed-step sequences with the existing `key_resolver`s are pure config (config/motifs.yaml + `MotifRegistry.reload()`, no redeploy); motifs needing new entity-linkage semantics get a new `KeyResolver` registered in `KEY_RESOLVERS` -- the same config-vs-code split already used for protocol lambda_p values vs. the decay formula's shape.)*

## Phase 4 — Cold Storage & Forensics (FR4)

- [x] 4.1 Design Neo4j graph schema for pruned-edge storage (nodes/relationships, indexed by entity id + timestamp). *(The `(Entity {id})-[:PRUNED_EDGE {...}]->(Entity {id})` shape and the `Entity.id`/`PRUNED_EDGE.pruned_at` indexes already came from task 2.4 (cold_storage.py); src/t_gnn/forensics.py adds the two indexes 4.2's query patterns actually need: `PRUNED_EDGE.t_e` (the *original event* time -- what design.md 2.7's "reconstruct activity in a time window" example reasons about, distinct from `pruned_at`'s eviction-time semantics already indexed for 2.4's audit use case) and `PRUNED_EDGE.edge_id` (point lookup, e.g. resolving a motif alert's `matched_edges` back to full metadata).)*
- [x] 4.2 Implement forensic query API/interface (e.g., "reconstruct activity around entity X in time window Y"). *(src/t_gnn/forensics.py `Neo4jForensicQueryAPI` -- `reconstruct_activity(entity_id, start, end)` implements design.md 2.7's example query verbatim (matches `entity_id` as either endpoint, filters/orders by `t_e`); `get_edge(edge_id)` is the complementary point lookup. Reads the same relationship shape `Neo4jColdStorageWriter` (2.4) writes -- no second schema. Genuinely wired against the real docker-compose Neo4j instance, not a stub.)*
- [x] 4.3 Verify pruned edges retain full original metadata (protocol, weight-at-prune-time, timestamps, endpoints). *(tests/test_forensics.py -- round-trips every `PrunedEdgeRecord` field through a real write + forensic read, including an end-to-end test that runs the real `PruningWatcher` and then reconstructs the pruned edge's full metadata via the forensic API, per FR4.2.)*

## Phase 5 — T-GNN Integration

- [x] 5.1 Customize PyTorch Geometric forward pass to read live Active Graph Store (dynamic edge dropping mid-stream). *(src/t_gnn/tgnn.py `DynamicTGNN.score_entities()` -- re-fetches `edge_index`/`node_index` from `ActiveGraphStore.to_pyg_edge_index()` (2.1) on every call rather than caching it, so an edge pruned since the last call is simply absent from the next forward pass. Per specs.md §4's non-goal excluding T-GNN model-architecture design, the model itself (two `SAGEConv` layers + a linear score head) is a deliberately small, untrained reference forward pass -- the integration seam is the deliverable, not a production-accurate detector.)*
- [x] 5.2 Wire deviation signal (1.5) into T-GNN as an input feature. *(src/t_gnn/tgnn.py `TGNNInferenceEngine.observe_deviation()` caches each entity's latest FR1.5 z-score; `DynamicTGNN.score_entities()` concatenates it onto that entity's stable base embedding (`EntityFeatureTable`) as an extra input column before the forward pass, so a deviation literally changes what the model sees, not just a side-channel annotation.)*
- [x] 5.3 Wire motif-completion signal (3.5) as a fast-path trigger for targeted/immediate inference. *(src/t_gnn/tgnn.py `TGNNInferenceEngine.on_motif_completion()` -- auto-subscribed to a `MotifAlertBus` (3.5) when passed to the constructor; scores only the completed motif's `chain_key` plus its live neighbors (`ActiveGraphStore.neighbors()`) immediately, rather than waiting for the next scheduled `run_once()` pass.)*
- [x] 5.4 End-to-end test: replay the LANL dataset (task 0.4) as a "low and slow" APT scenario and confirm detection via baseline deviation. *(tests/test_tgnn_e2e.py `test_lanl_replay_low_and_slow_apt_detected_via_baseline_deviation` -- stages the sample LANL fixture (0.4) as real background traffic through `DecayStreamProcessor`, layers a synthetic low-and-slow tail onto one of its entities, and confirms FR1.5's z-score flags the tail's anomalous final event (\|z\|>3) while the normal tail stays unremarkable; also confirms the resulting `DeviationSignal` feeds through `TGNNInferenceEngine` per 5.2.)*
- [x] 5.5 End-to-end test: inject a synthetic motif-matching attack sequence and confirm motif-completion alert fires. *(tests/test_tgnn_e2e.py `test_synthetic_lateral_pivot_attack_fires_alert_and_triggers_fast_path_inference` -- replays the canonical two-hop lateral-pivot sequence through a real `MotifEngine`, confirms the `MotifCompletionEvent` fires only after both hops, and confirms that alert immediately drove `TGNNInferenceEngine`'s fast path (5.3) via the shared `MotifAlertBus` with no extra wiring beyond construction.)*

## Phase 6 — Observability & Hardening

- [x] 6.1 Implement audit logging for all prune events and motif resets (NFR5). *(src/t_gnn/audit.py `AuditLogger`/`AuditSink`/`FileAuditSink`/`InMemoryAuditSink` -- subscribes to `pruning.py`'s `PruneEventBus` and `motif_engine.py`'s new `MotifResetBus` (added this phase) and writes one newline-delimited JSON record per event, the same "no new infra beyond what's needed" posture the rest of Phase 6 takes.)*
- [x] 6.2 Implement dashboards/metrics: active graph size, prune rate, ε over time, motif cache hit/reset rate, inference latency. *(src/t_gnn/metrics.py `MetricsCollector`/`RollingRateCounter` -- active graph size read live from the store; prune/motif-hit/motif-reset rates from bus subscriptions (`PruneEventBus`/`MotifAlertBus`/`MotifResetBus`); epsilon-history and inference-latency series via `observe_pruning_pass()`/`observe_inference_pass()`, called explicitly by whoever drives `PruningWatcher.run_once()`/`TGNNInferenceEngine` passes since both already return everything needed without a new hook. `snapshot()` is the dashboard-ready read; no Prometheus/Grafana integration since design.md doesn't name one and none is provisioned in docker-compose.yml.)*
- [x] 6.3 Implement graceful-degradation behavior for Redis unavailability (motif detection disabled, anomaly detection unaffected). *(src/t_gnn/motif_engine.py -- every `MotifEngine` call into `state_store` now goes through `_state_get`/`_state_set`/`_state_delete`/`_state_containing_edge`, which catch `redis.exceptions.RedisError` and flip `self.available` to `False` (logged once, not per edge) instead of propagating; the next successful call flips it back. `BaselineStore`/`DecayEngine`/`TGNNInferenceEngine` have no Redis dependency in the first place, so FR1.5 anomaly detection is unaffected by construction, not special-cased here.)*
- [x] 6.4 Implement buffering for Neo4j write-path so cold-storage latency never blocks pruning. *(src/t_gnn/cold_storage.py `BufferedColdStorageWriter` -- `write()` only enqueues (non-blocking); a background drain thread (`start()`/`stop()`, or synchronous `drain_once()` for tests) performs the real write against the wrapped writer, retrying up to `max_retries` times before logging a data-loss warning and dropping the record. Drop-in `ColdStorageWriter`, so `PruningWatcher` itself needed zero changes. Deliberately trades 2.4's stronger "write acknowledged before removal" guarantee for pruning throughput under a Neo4j slowdown -- the explicit tradeoff task 2.4's docstring deferred to this task.)*
- [x] 6.5 Chaos/failure testing: Flink backpressure, Redis outage, Neo4j slow writes — verify mitigations from design.md §5. *(tests/test_chaos.py -- one test per design.md §5 failure-mode row: a 1000-edge ingest spike showing epsilon tighten toward epsilon_max under size pressure then relax once calm; a simulated Redis outage showing motif detection disable cleanly while FR1.5 baseline-deviation detection proceeds unaffected on the same edges; intermittent Neo4j latency spikes showing `PruningWatcher.run_once()` never stalls and every write eventually lands via `BufferedColdStorageWriter`; and a misconfigured λ_p causing a visible prune-rate spike in the audit log, corrected via `ProtocolDecayRegistry.reload()` with no redeploy. The fourth row's "incorrect λ_p" mitigation itself (design.md 2.2/tasks.md 1.2's hot-reload) predates this phase; this task's contribution is verifying it under a simulated fault, not implementing it.)*

## Phase 7 — Documentation & Rollout

- [x] 7.1 Document configuration reference (decay constants, ε tuning, motif definition format). *(docs/configuration-reference.md -- every config surface introduced across all phases: config/protocols.yaml, `EpsilonController`, config/motifs.yaml + motif.schema.json, `Neo4jConfig`, the Redis client, `BufferedColdStorageWriter`, `MetricsCollector`, `AuditLogger`, the T-GNN model/engine, and .env.example.)*
- [x] 7.2 Document operational runbook (how to tune `λ_p`, add new motifs, investigate forensic queries). *(docs/operational-runbook.md -- step-by-step procedures for tuning λ_p, adding a motif (config-driven vs. code-driven per 3.9), investigating forensic queries (programmatic + Cypher), monitoring via metrics/audit log, responding to a Redis outage (6.3) or Neo4j slowdown (6.4), and running a pilot evaluation (7.3).)*
- [x] 7.3 Pilot deployment against a subset of enterprise log traffic; validate false-positive/negative rates before full rollout. *(src/t_gnn/pilot.py -- a real, tested harness (`evaluate_anomaly_detection()`/`evaluate_motif_detection()`/`run_pilot()`) computing true/false positive/negative rates for both detection paths against LANL-format `redteam.txt` ground truth, plus a `python -m t_gnn.pilot` CLI mirroring `calibrate_decay.py`'s. `data/lanl/raw/sample_redteam.txt` + `tests/test_pilot.py` exercise the harness end-to-end against the tiny sample LANL fixture -- correctly reporting a miss, not a false detection, since 5 rows isn't enough history for a z-score and has no Machine->Machine hop for either seed motif. The pilot itself -- running this against a real labeled subset of enterprise traffic and using the resulting rates for a go/no-go rollout call -- is the operational step this repo can't perform (no live deployment, no real labeled data), the same acquisition gap task 0.4 already documents for the LANL dataset itself. Extension: src/t_gnn/data/simulate_traffic.py generates *synthetic* labeled traffic at a configurable scale -- benign background noise plus injected, verifiably-detectable instances of both seed motifs and a low-and-slow anomaly -- for exercising the harness/pipeline locally beyond the tiny fixture; still not a substitute for a real pilot against real enterprise traffic.)*

## Phase 8 — Tooling & Documentation Follow-ups

- [x] 8.1 Build a CLI/script that drives staged or simulated edges through the actual live T-GNN model (`src/t_gnn/tgnn.py`'s `DynamicTGNN`/`TGNNInferenceEngine`, Phase 5) and prints per-entity inference scores. *(src/t_gnn/score_entities.py `score_staged_edges()` + `python -m t_gnn.score_entities` CLI -- closes the gap that `src/t_gnn/pilot.py` (7.3) evaluates only the decay/baseline-deviation path (FR1.5) and the motif-completion path (FR3.4) against labeled ground truth without ever invoking the PyTorch Geometric forward pass design.md §2.8 describes. Reuses Phase 5's pieces as-is -- no engine/model changes: staged edges (`stage_lanl.py` or `simulate_traffic.py` output) are replayed through a real `DecayStreamProcessor` (1.3-1.5) and `MotifEngine` (3.2-3.5) to populate live state in an `ActiveGraphStore` (2.1), with `TGNNInferenceEngine` wired to the same `MotifAlertBus` so motif completions during replay drive the 5.3 fast path exactly as design.md's data flow (§3, steps 5-7) describes, before a final scheduled pass (5.1/5.2) scores every entity left in the graph, sorted by score magnitude. Per specs.md §4's non-goal, this surfaces the existing untrained reference model's output -- it does not change or train the model itself.)*
- [x] 8.2 Fix PowerShell-incompatible bash-style line-continuation (`\`) in `docs/operational-runbook.md` and this file's — CLAUDE.md's — Commands section for the `simulate_traffic`/`pilot` invocations. *(Root-caused during local CLI verification: Windows PowerShell doesn't accept `\` for line continuation, which produced a misleading "module not found"/argparse-looking error that was actually a shell-syntax problem, not an install/import problem. Replaced the multi-line bash-style examples with PowerShell-safe backtick-continued forms in both docs so the same trap doesn't recur for other commands.)*

## Backlog — Actionable Extensions (tracked, ready to pick up)

Gaps identified by cross-referencing the current code against `functionality.txt`, `specs.md`, `design.md`, and `Protocol_Aware_Temporal_Graph_Analytics_Proposal.docx`. Excludes specs.md §4's explicit non-goals (replacing the T-GNN model architecture, SOAR/alert-triage automation, multi-tenant graph federation) — those are deliberately out of scope, not oversights, so they are not listed here as work to pick up.

- [ ] B.1 Stand up a real Apache Flink streaming job that runs the existing decay/baseline logic (design.md §2.1) as actual keyed operators, including true broadcast-state distribution of `λ_p` to task managers (design.md §2.2) instead of a per-process YAML reload. *(Gap: `docker-compose.yml` brings the Flink cluster up, but nothing is ever submitted to it — `DecayEngine`/`BaselineStore`/`DecayStreamProcessor`/`ProtocolDecayRegistry` are framework-agnostic Python standing in for what would be Flink's job, per CLAUDE.md's Phase 1 notes. This is the prerequisite for B.2 below. **Environment gap, deliberately not attempted**: PyFlink for this stack's Flink 1.18 only supports Python 3.8-3.10, but this dev environment runs Python 3.12 -- and there's no message broker (Kafka or similar) anywhere in `docker-compose.yml` for a job to actually consume from, so even a compatible PyFlink install would have no concrete source to attach to. Confirmed via `pip show apache-flink` (not installed) and `docker compose ps` (stack not currently up) before deciding this, rather than guessing. Attempting this would need a separate Python 3.10 virtualenv plus a source-connector decision (file-watch vs. socket vs. adding a broker to the compose stack) -- a larger, riskier undertaking than this backlog pass's other items, so it's left unattempted rather than half-built. Revisit once either the dev machine's Python version changes or a message broker is added to the stack.)*
- [ ] B.2 Wire a real Flink backpressure metric into `EpsilonController` so `ε` tightens under actual ingestion backpressure, not just memory/graph-size pressure (design.md §5's "Flink backpressure during log spikes" mitigation). *(Depends on B.1 — there is no Flink job to backpressure without it; `tests/test_chaos.py`'s ingest-volume-spike test is an explicit proxy for this today. Blocked on the same environment gap as B.1.)*
- [x] B.3 Continuous/adaptive `λ_p` learning from live traffic, replacing (or supplementing) `calibrate_decay.py`'s one-shot offline heuristic with a mechanism that updates decay constants as network usage patterns evolve. *(Proposal.docx §7 "Future Enhancements": "learning protocol-specific decay behavior directly from historical data rather than setting it through fixed constants." src/t_gnn/adaptive_calibration.py `AdaptiveDecayCalibrator`/`RecalibrationEvent` -- `observe()` is fed one live edge at a time (the same "caller invokes explicitly alongside the per-edge step" pattern `metrics.py`'s `observe_pruning_pass()` uses), tracking a rolling per-protocol window of same-entity inter-arrival gaps and reusing `calibrate_decay.py`'s exact median-gap heuristic (`lambda_p = ln(2) / median_gap`) every `update_interval_edges` edges once a protocol clears `min_samples`. A `max_relative_change` clamp bounds how far one recalibration can move `lambda_p`, since a noisy live window is a less reliable signal than a whole-dataset batch view -- guards against design.md §5's "Incorrect lambda_p" failure mode. Writes go through a new `ProtocolDecayRegistry.update()` (in-memory only, preserves existing `half_life_hours`/`description` metadata) -- a human-driven correction via task 1.7 still hand-edits the YAML + `reload()`s; this is the separate always-on path. tests/test_adaptive_calibration.py + tests/test_protocol_registry.py.)*
- [x] B.4 Probabilistic/fuzzy motif matching as an extension to `motifs.py`/`motif_engine.py`'s current exact-structural-match-only mechanism (exact `edge_type`/`protocol`/`src_type`/`dst_type` set membership plus exact key equality), to catch variations of known attack techniques rather than only exact sequences. *(Proposal.docx §7. `motifs.py`'s `MotifStep.match_score()` -- `src_type`/`dst_type` still hard-reject when specified (they encode the pattern's structural roles, not a substitutable technique), but `edge_type`/`protocol` contribute partial credit (e.g. an attacker using SMB where the canonical pattern used RDP) instead of an all-or-nothing reject, returning a score in `(0, 1]` or `None`. `motif_engine.py`'s `MotifEngine` gained `fuzzy`/`min_confidence` constructor params -- `fuzzy=False` (default) is byte-for-byte the pre-B.4 exact-match behavior (verified: all pre-existing motif tests pass unchanged); `fuzzy=True` uses `match_score()` instead, accumulates a chain's confidence as the running product of each step's score, and only emits a `MotifCompletionEvent` (now carrying a `confidence` field, default 1.0) if the final product clears `min_confidence` -- otherwise the completed-but-low-confidence state is dropped rather than reported. `MotifState` gained the same `confidence` field (default 1.0), persisted by `RedisMotifStateStore` with a backward-compatible read default for pre-B.4 records. tests/test_motifs.py (match_score unit tests) + tests/test_motif_fuzzy.py (engine integration, including confirming non-fuzzy mode ignores `min_confidence` entirely).)*
- [x] B.5 Distribute the Active Graph Store (`graph_store.py`) and the motif-state cache across multiple nodes. *(Proposal.docx §7: "distributing the active graph and pattern cache across multiple nodes to support even larger deployments." New src/t_gnn/sharding.py `stable_shard_index()` -- a SHA-256-based hash stable across processes (unlike Python's per-process-randomized `hash()`), the one primitive both distributed wrappers need. `graph_store.py`'s `ShardedActiveGraphStore` partitions N `ActiveGraphStore` shards by `edge_id` (itself already a stable hash, `schema.py`'s `make_edge_id()`) -- `upsert`/`remove`/`get` route directly to one shard with no cross-shard bookkeeping, while `neighbors()`/`to_pyg_edge_index()` fan out across all shards and merge (a real scatter-gather tradeoff, not requiring a node-to-shard directory service a vertex-partitioned scheme would need). `motif_engine.py`'s `ShardedMotifStateStore` wraps N `MotifStateStore`s (e.g. N `RedisMotifStateStore`s each pointed at a different Redis host/db for genuine multi-node use, or `InMemoryMotifStateStore`s for tests), routing `get`/`set`/`delete` by `chain_key` and fanning `states_containing_edge()` out across all shards. Each shard is an ordinary, already-real store -- in a genuinely multi-node deployment they'd be separate processes/machines behind an RPC layer; here they're in-process, the same relationship `InMemoryColdStorageWriter` has to a real distributed writer, except the partitioning/routing logic itself is real and directly tested. tests/test_sharding.py, including an end-to-end `MotifEngine` completion test using a sharded state store.)*
- [ ] B.7 Validate NFR1 (sub-millisecond inference latency) and NFR2 (billions of events/day throughput) against real enterprise-scale load, not proxy-scale synthetic load. *(`tests/test_load.py` is explicitly labeled a proxy-scale smoke test — e.g. `store.get()` stands in for a real inference read, and ingest volume is far below "billions of events/day." Blocked on the same real-traffic/real-infrastructure acquisition gap task 0.4 and 7.3 already document — no amount of local benchmarking substitutes for that, so this stays unchecked. Partial progress: tests/test_load.py gained two opt-in (`RUN_HEAVY_LOAD_TEST=1`, skipped by default so the normal suite stays fast) tests at ~25-100x the original smoke tests' scale (125k edges ingested / 200k edges resident vs. 4k/2k) -- still a local proxy, not enterprise-scale validation, but a meaningfully larger one than the default suite exercises.)*
- [ ] B.8 Ingestion adapter for OTRF/Security-Datasets ("Mordor") — a public collection of real Sysmon + Windows Security event captures of actual attack techniques (not synthetic like `simulate_traffic.py`), identified while researching public datasets that could replace/supplement the tiny vendored LANL sample. *(Implemented (`src/t_gnn/data/stage_mordor.py` `stage()`/`normalize()`/`parse_mordor_stream()`, `data/mordor/`, `tests/test_stage_mordor.py`) and verified end-to-end against a real downloaded capture (`purplesharp_ad_playbook_I` — 229 real edges extracted from a 26k-line file, including the actual auth→exec lateral-movement sequence, run through the full decay/baseline/motif/T-GNN pipeline via `score_entities.py`), but kept on the `feature/mordor-ingestion` branch rather than merged into `main` at the developer's request — unchecked here because `main` itself doesn't carry this adapter. See that branch's `tasks.md`/`CLAUDE.md` for the full implementation notes.)*

## Frontend Implementation — React SOC Dashboard

Tracked on the `frontend_implementation` branch (not merged to `main`). Stack:
**React + TypeScript + Vite** — nothing in this repo dictates a different
frontend stack (no existing frontend code, no framework pinned in
`specs.md`/`design.md`). State/data-fetching: TanStack Query for server-state
caching (matches the "caching/retry/pagination" requirements below without
hand-rolling them) + a lightweight client-state store (Zustand or React
Context, decided in F1). Charting: pick one library in F1 (e.g. Recharts or
visx) rather than mixing — see F12.

**Ground truth this roadmap is built against**: this repo currently exposes
its functionality *only* as a Python library (`src/t_gnn/*.py`) plus one-shot
CLI tools (`docs/cli-reference.md`) that print to stdout or write flat
artifacts (`scores.json`, `pilot-report.json`, `logs/audit.log`,
newline-delimited) and a long-running-but-not-served process
(`scripts/run_pipeline.py`, prints to stdout/log file, no socket). **There is
no HTTP/WebSocket API server anywhere in the codebase today** (confirmed: no
FastAPI/Flask/uvicorn/websockets dependency or code exists). There is also no
authentication/user/session model, no IP-address or device field in the edge
schema (`src/t_gnn/schema.py`'s `NODE_TYPES = ("User", "Machine")` only), no
geographic/location data anywhere in `Edge`/`PrunedEdgeRecord`, and no
persisted concept of an aggregate "company security score." Per this task's
instructions, none of these are invented below — every task either cites the
real module/CLI it will surface, or is explicitly marked **`[BACKEND TODO]`**
for functionality that does not exist yet and needs a backend design
decision before frontend work can consume it for real (a mock/fixture can
unblock UI work in the meantime; see F0.9).

### Milestone F0 — Backend API Layer (blocking prerequisite)

Nothing downstream can show real data until this exists. Scope: a thin
HTTP(+WebSocket) service that wraps the existing engine objects — it should
not reimplement detection logic, only expose it.

**Architecture decisions (made with the developer, 2026-07-30):** process
model is **decoupled** — the API service never embeds
`ActiveGraphStore`/`MotifEngine`/`TGNNInferenceEngine` itself; a separate,
long-running pipeline process (`scripts/run_pipeline.py`) persists its live
state and the API only reads it back. Persistence for everything the API
layer owns (that isn't already Neo4j/Redis's job) is the existing,
previously-idle local **Postgres** (`t_gnn_dev`, `src/t_gnn/db.py`) —
reused rather than standing up a second database, per CLAUDE.md's existing
guidance to use it for persistence needs outside Neo4j/Redis's roles.
Framework is **FastAPI + uvicorn** (`src/t_gnn/api/`), since it imports
`src/t_gnn`'s existing modules directly with no cross-language bridge and
gets OpenAPI schema generation (F0.15) for free. F0.11 (real auth) is
**deferred** for this pass — F3.4's mock-auth bypass unblocks frontend work
in the meantime; the `users` table is still created now so F9.5/F13.6 have
a real foreign key to attribute to once real login lands.

- [x] F0.1 Decide and stand up the API framework/process model (e.g. FastAPI + uvicorn under `src/t_gnn/api/` or a sibling `backend_api/` package). Decide whether it hosts long-lived engine instances itself (`ActiveGraphStore`, `MetricsCollector`, `TGNNInferenceEngine`, etc., wired the same way `scripts/run_pipeline.py` already does) or attaches to a separately-running `run_pipeline.py` process via a shared store (Redis/Neo4j are already shared; in-process bus state like `MetricsCollector`/`AuditLogger` is not). *(Decided: decoupled process model, FastAPI + uvicorn, Postgres as the bridge — see the architecture-decisions note above. `src/t_gnn/api/app.py` (`create_app()`), `src/t_gnn/api/deps.py` (dependency providers), `src/t_gnn/api/__main__.py` (`python -m t_gnn.api`). `src/t_gnn/api_state.py` is the new Postgres bridge: `create_api_tables()` (DDL for `users`/`metrics_snapshots`/`entity_scores`/`motif_completions`/`motif_resets`/`motif_feedback`/`alert_acknowledgements`), `ApiStateWriter` (auto-subscribes to `MotifAlertBus`/`MotifResetBus`/`InferenceResultBus`/`MotifFeedbackBus`, the same auto-subscribe convention `AuditLogger` uses; degrades gracefully — `self.available` flips false, logged once — on a Postgres outage, mirroring 6.3's Redis-outage handling), `ApiStateReader` (the query side the API routers use). Wired into `scripts/run_pipeline.py` behind `--no-api-persist` (on by default); `scripts/init_postgres.py` now also creates these tables. Tests: `tests/test_api_state.py` (live-Postgres, skip-if-unreachable per this repo's existing convention) + `tests/test_api.py` (HTTP layer via `TestClient` + `dependency_overrides`, no live infra needed).)* — Complexity: L — Depends on: none.
- [x] F0.2 `GET /api/metrics/snapshot` — active graph size, prune rate, ε, motif hit/reset rate, latest inference latency. *(Backing: `src/t_gnn/metrics.py` `MetricsCollector.snapshot()` → `MetricsSnapshot`. Implemented: `src/t_gnn/api/routers/metrics.py`, reading `ApiStateReader.latest_metrics_snapshot()`; 404 if none recorded yet.)* — Complexity: S — Depends on: F0.1.
- [x] F0.3 `GET /api/scores/entities` — latest per-entity T-GNN scores, paginated/sortable by `abs(score)`. *(Backing: `src/t_gnn/score_entities.py` `score_staged_edges()` / `src/t_gnn/tgnn.py` `InferenceResult{entity_id, score, t, trigger, motif_name}` — the exact shape already dumped to `scores.json`. Implemented: `src/t_gnn/api/routers/scores.py`; rows populated by `ApiStateWriter.record_entity_score()` subscribed to `InferenceResultBus`, upserted per entity (latest only).)* — Complexity: M — Depends on: F0.1.
- [x] F0.4 `GET /api/motifs/completions` (+ live stream variant, F0.10) — recent `MotifCompletionEvent`s: `motif_name, chain_key, matched_edges, completed_at, confidence`. *(Backing: `src/t_gnn/motif_engine.py` `MotifAlertBus`. Implemented: `src/t_gnn/api/routers/motifs.py` (`GET`/paginated, optional `motif_name` filter) — live-stream variant now implemented too, see F0.10.)* — Complexity: M — Depends on: F0.1.
- [x] F0.5 `GET /api/motifs/resets` — recent `MotifResetEvent`s, for the Detection Matrix's reset/false-positive-adjacent signal. *(Backing: `MotifResetBus`. Implemented: `src/t_gnn/api/routers/motifs.py`.)* — Complexity: S — Depends on: F0.1.
- [x] F0.6 `GET /api/forensics/entity/{entity_id}?start=&end=` — reconstructed pruned-edge activity timeline for an entity. *(Backing: `src/t_gnn/forensics.py` `Neo4jForensicQueryAPI.reconstruct_activity()` → list of `PrunedEdgeRecord{edge_id, src, dst, edge_type, protocol, t_e, w_at_prune, pruned_at, source_system, raw_event_id}`.)* This is the real data source for User Investigation's "timeline of activity" and "log history" (F10) — there is no separate raw-log store. *(Implemented: `src/t_gnn/api/routers/forensics.py`, a direct wrapper — `get_forensics_api()` (deps.py) raises a clean 503 instead of crashing the service if Neo4j isn't reachable.)* — Complexity: M — Depends on: F0.1.
- [x] F0.7 `GET /api/forensics/edge/{edge_id}` — single pruned-edge point lookup, for resolving a motif's `matched_edges` ids to full metadata. *(Backing: `Neo4jForensicQueryAPI.get_edge()`. Implemented alongside F0.6; 404 if the edge was never pruned.)* — Complexity: S — Depends on: F0.1.
- [x] F0.8 `GET /api/audit/log?since=&type=` — tail/paginate the audit log (`type: prune | motif_reset` records) for the Log Explorer. *(Backing: `src/t_gnn/audit.py` `FileAuditSink` writing NDJSON to `logs/audit.log`. Implemented: `src/t_gnn/audit.py`'s new `read_records()` (file scan + `since`/`record_type` filter, newest-first), `src/t_gnn/api/routers/audit.py`, `src/t_gnn/api/deps.py`'s `audit_log_path()` reading `AUDIT_LOG_PATH` env var (default `logs/audit.log`, mirroring `scripts/run_pipeline.py --audit-log`'s default) — no live-infra dependency, since the audit log is a plain file.)* Note: this is prune/reset audit records, not raw Sysmon/Windows Event Log entries — the repo has no raw-log persistence layer; flag as a gap if "view raw logs" (per the Log Explorer requirement) means the original ingested event, not the audit trail. — Complexity: M — Depends on: F0.1.
- [x] F0.9 `GET /api/config/protocols` and `GET /api/config/motifs` — read-only surfacing of `config/protocols.yaml` (λ_p per protocol) and `config/motifs.yaml` (the motif library), for Settings (F15) and the Detection Matrix's "detection category" column. *(Backing: `src/t_gnn/protocol_registry.py` `ProtocolDecayRegistry`, `src/t_gnn/motifs.py` `MotifRegistry`. Implemented: `src/t_gnn/api/routers/config.py`, reading the real hot-reloadable registries directly — no Postgres involved, so this endpoint has no live-infra dependency at all.)* — Complexity: S — Depends on: F0.1.
- [x] F0.10 WebSocket (or SSE) channel streaming `MotifCompletionEvent`/`PrunedEdgeEvent`/`InferenceResult` as they occur, for Live Monitoring (F13). *(Backing: same buses as F0.2-F0.5; no existing push transport — this is new plumbing, not just a new consumer of an existing one. Implemented as SSE, not WebSocket, since the channel is one-directional and needs no extra dependency beyond FastAPI/Starlette: `src/t_gnn/api/routers/stream.py`'s `GET /api/stream/events`. Honest caveat: given F0's decoupled-process architecture, the API process has no live bus to relay from directly — this polls `ApiStateReader`'s new `list_motif_completions_since()`/`list_motif_resets_since()`/`list_entity_scores_since()` (Postgres, id/updated_at cursors) and `audit.py`'s `read_records(since=...)` (the audit log file, for prune events) on a configurable interval (`STREAM_POLL_INTERVAL_SECONDS`, default 1s via `deps.get_stream_config()`) and re-emits new rows as SSE events (`motif_completion`/`motif_reset`/`inference_result`/`prune`/`heartbeat`) — a low-latency push experience built on polling, not a zero-latency bus relay. Postgres errors mid-stream surface as an `error` SSE event and the poll loop keeps retrying rather than the connection dying.)* — Complexity: L — Depends on: F0.1, F0.4, F0.5.
- [~] F0.11 `[BACKEND TODO]` Authentication/authorization — no user/session/role model exists anywhere in this repo (it's a detection pipeline, not a multi-user service). Define: who are "users" of the dashboard (SOC analysts vs. admins), what they authenticate against (local user store? SSO/OIDC?), and what's authorized differently (e.g. who can acknowledge alerts (F13) or submit motif-feedback dispositions (`src/t_gnn/feedback.py` `MotifFeedbackBus` already exists for the latter — this task is the auth/identity wrapper around it, not the tracking logic itself)). *(Decided: defer real login, keep F3.4's mock-auth bypass. `users` table (`api_state.py`) created now — `username`/`display_name`/`password_hash`/`role`/`created_at` — so `motif_feedback`/`alert_acknowledgements` have a real FK to attribute to (`ApiStateWriter.get_or_create_user()` inserts a placeholder row keyed on whatever free-text `analyst` string the mock-auth frontend sends). Real login (password hashing, sessions/JWT, SSO/OIDC) is still not built.)* — Complexity: L — Depends on: F0.1.
- [ ] F0.12 `[BACKEND TODO]` "Company cybersecurity score" / overall security posture. No aggregate score is computed or persisted anywhere today — `specs.md`/`design.md`/`functionality.txt` don't define one. Before F6/F14 can show a real number, decide the formula (e.g. a weighted function of prune rate, motif hit rate, mean/max entity score magnitude, open-alert count from `MetricsSnapshot` + recent `InferenceResult`s) and where it's computed (new backend aggregation job vs. client-side derived metric — prefer backend, so the definition is centralized and auditable). *(Unblocked but not started: `metrics_snapshots`/`entity_scores` history now exists in Postgres for whatever formula gets decided to read from.)* — Complexity: M — Depends on: F0.2, F0.3.
- [ ] F0.13 `[BACKEND TODO]` IP address / device / session-history fields. `Edge`'s node ids are `User:<name>` / `Machine:<name>` only (`schema.py` `NODE_TYPES`) — there is no IP, device-fingerprint, or session-id field anywhere in the edge or pruned-edge schema. If User Investigation (F10) must show these, this requires a schema change to `config/schema/edge.schema.json` + `Edge` (a cross-cutting change touching every ingestion adapter, not a frontend-only gap) — flag to backend/data-model owners rather than fabricating these fields in the API layer. — Complexity: M (decision) / L (if schema change approved) — Depends on: none.
- [ ] F0.14 `[BACKEND TODO]` Geographic attack map data. No location/geo field exists in any data model in this repo. Either descope the "Geographic attack map" visualization (F12) or define where geo data would come from (e.g. IP geolocation, which itself depends on F0.13 first existing) — do not fabricate coordinates. — Complexity: S (decision) — Depends on: F0.13.
- [x] F0.15 API error/response conventions: consistent error envelope, pagination envelope (cursor or offset — pick one, apply everywhere F0.3/F0.4/F0.6/F0.8 paginate), and OpenAPI schema generation so the frontend can codegen types. *(Implemented: `src/t_gnn/api/app.py`'s exception handlers → `{"error": {"code": ..., "message": ...}}` for every `HTTPException`/unhandled exception; `src/t_gnn/api/schemas.py`'s `Paginated[T]` (`items`/`limit`/`offset`/`total`) applied by every list endpoint, offset-based (cursor-based was considered and rejected — none of this data needs cursor stability across concurrent writes at this scale). OpenAPI schema comes free from FastAPI at `/openapi.json`.)*

  Also implemented ahead of schedule while building F9.5's groundwork: `GET`/`POST /api/motifs/feedback` (analyst true/false-positive dispositions, persisted via the new `motif_feedback` table) and `POST /api/alerts/ack` (F13.6's alert acknowledgement, `alert_acknowledgements` table) — both listed under their respective milestones below, not re-listed here. — Complexity: M — Depends on: F0.1.

### Milestone F1 — Project Setup

- [x] F1.1 Scaffold Vite + React + TypeScript app under `frontend/`. *(Implemented: `npm create vite@latest frontend -- --template react-ts` -- React 19/Vite 8/TypeScript 6. Vite's default marketing-template content (App.tsx boilerplate, hero/logo assets, oxlint) was stripped/replaced since it doesn't fit a SOC dashboard and F2/F5 build the real shell/pages anyway; see `frontend/README.md`.)* — Complexity: S — Depends on: none.
- [x] F1.2 Establish lint/format tooling (ESLint + Prettier) and commit hooks; note in root `README.md`'s Commands section once this exists (currently "no lint/format/build step configured" applies to the Python side only). — Complexity: S — Depends on: F1.1. *(Implemented: the template's default `oxlint` swapped for ESLint (flat config, `typescript-eslint` + `eslint-plugin-react-hooks`/`react-refresh` + `eslint-config-prettier`) plus Prettier (`.prettierrc.json`). Husky + lint-staged run `eslint --fix`/`prettier --write` on staged files pre-commit -- `frontend/package.json`'s `prepare` script re-points git's `core.hooksPath` at `frontend/.husky` (relative to the repo root, since `frontend/` is a subdirectory of this repo, not its own git root) on every `npm install`. Root `README.md`'s Commands section now points at `frontend/README.md` for the frontend-specific command list rather than duplicating it.)*
- [x] F1.3 Configure path aliases, environment variable handling (`VITE_API_BASE_URL`, etc.), and a `.env.example` for the frontend (mirroring the repo's existing `.env`/`.env.example` convention for the Python backend). — Complexity: S — Depends on: F1.1. *(Implemented: `@/*` -> `src/*` alias in both `vite.config.ts`'s `resolve.alias` and `tsconfig.app.json`'s `paths` (no `baseUrl` -- deprecated in TS 6.0). `src/config/env.ts` reads `import.meta.env.VITE_API_BASE_URL` (typed via `src/vite-env.d.ts`) and throws a clear error if unset rather than silently defaulting to a wrong backend. `frontend/.env.example` documents it (default `http://127.0.0.1:8000`, matching `python -m t_gnn.api`'s default); real local values go in the already-gitignored `.env.local` (Vite's own `*.local` convention, not a new gitignore entry).)*
- [x] F1.4 Choose and install the charting library (F12 needs this decided first) and the client-state library (Zustand or Context+reducer). — Complexity: S — Depends on: F1.1. *(Decided: Recharts (charting) and Zustand (client state) -- both installed. Not wired into any component yet since no real feature needing them exists before F5/F12; premature example code would just be thrown away when those milestones land.)*
- [x] F1.5 Set up TanStack Query (or equivalent) as the server-state layer used by F4. — Complexity: S — Depends on: F1.1. *(Implemented: `@tanstack/react-query`'s `QueryClientProvider` wraps `<App />` in `src/main.tsx` with a module-level `QueryClient` -- real structural wiring, not just an installed-but-unused dependency, since F4's hooks depend on a provider already being mounted.)*
- [x] F1.6 Base folder structure: `pages/`, `components/`, `features/`, `api/`, `hooks/`, `store/`, `types/`. — Complexity: S — Depends on: F1.1. *(Implemented under `frontend/src/` with `.gitkeep` placeholders (git doesn't track empty directories); a `config/` folder was added alongside the specified seven for `env.ts` (F1.3) -- documented in `frontend/README.md`'s folder-structure table.)*
- [x] F1.7 CI: add a frontend build/lint/test job (new, since none exists today — the repo's only CI-relevant command today is `pytest`). — Complexity: M — Depends on: F1.1, F1.2. *(Implemented: `.github/workflows/frontend-ci.yml` -- the repo's first GitHub Actions workflow of any kind. Triggers on pushes to `frontend_implementation` and PRs touching `frontend/**`; runs `npm ci` then lint/format-check/test (Vitest + React Testing Library, one smoke test in `src/App.test.tsx`)/build in sequence. `working-directory: frontend` via `defaults.run` since the app lives in a subdirectory, not the repo root.)*

### Milestone F2 — Routing & App Shell

- [x] F2.1 Install/configure React Router; define top-level routes: `/`, `/analytics`, `/investigation/:entityId`, `/detections`, `/logs`, `/monitoring`, `/settings`, `/login`. — Complexity: S — Depends on: F1.1. *(Implemented: `react-router-dom@7` (data router, `createBrowserRouter`). `src/config/routes.ts`'s `ROUTES` is the single source of truth for every path -- `NAV_ROUTES` (a subset: excludes `/investigation/:entityId`, a drill-down detail route, and `/login`, outside the authenticated shell) feeds F5.2's Sidebar so nav links can't drift out of sync with the route list. `src/router.tsx`'s `routes` array is exported separately from the `router` (`createBrowserRouter(routes)`) instance so tests can feed the same tree into `createMemoryRouter` instead of touching real browser history. `npm audit` flags a high-severity react-router advisory (GHSA-qwww-vcr4-c8h2) affecting *RSC-mode* action execution -- this app uses a plain client-side data router with no server actions/RSC mode, so that code path is never reached; the only "fix" `npm audit` offers is downgrading below 7.12.0, which would be a regression for an inapplicable vulnerability class, so the current 7.18.2 was kept.)*
- [x] F2.2 App shell layout: Navbar + Sidebar + content outlet (components built in F5). — Complexity: M — Depends on: F1.6, F5.1, F5.2. *(F5.1/F5.2 pulled forward into this pass -- see below -- since F2.2 has a hard dependency on them and both are Complexity S with no unmet dependencies of their own. Implemented: `src/components/AppShell.tsx`, the root layout route -- `Navbar` on top, `Sidebar` + `<Outlet/>` below. Not a child of `/login`'s route, so the login page renders without nav chrome.)*
- [x] F2.3 Route-level code splitting (`React.lazy`/dynamic import per page). — Complexity: S — Depends on: F2.1. *(Implemented via React Router v7's native `lazy` route field (dynamic `import()` per page resolving a `Component` export) rather than manual `React.lazy`/`Suspense` -- the data router already awaits it during navigation. Verified concretely, not just configured: `npm run build`'s output shows each page as its own chunk (`HomePage-*.js`, `AnalyticsPage-*.js`, etc.), not one bundle.)*
- [x] F2.4 Protected-route wrapper gating on auth state (stubbed until F3/F0.11 land; behind a feature flag if backend auth isn't ready yet). — Complexity: S — Depends on: F3.1. *(Implemented as a pass-through layout route, `src/components/ProtectedRoute.tsx` -- every protected page is already nested under it in `router.tsx`, so F3.1 only needs to add the real redirect-to-`/login` check (and/or F3.4's mock-auth bypass) here, not re-nest any routes. Deliberately not a feature flag toggling "real" vs. "fake" gating, since no auth state exists anywhere yet for either branch to check -- there would be nothing to toggle.)*
- [x] F2.5 404 / error-boundary route. — Complexity: S — Depends on: F2.1. *(Implemented: `src/pages/NotFoundPage.tsx` as the `path: '*'` route (a matched-but-nonexistent path) and `src/components/RouteErrorBoundary.tsx` as the root layout route's `errorElement` (a thrown render/loader/lazy-import error) -- two distinct failure modes, not one component covering both.)*

Pulled forward from Milestone F5 (both Complexity S, depended on only by F2.2, already-satisfied F1.4 dependency):

- [x] F5.1 Navbar. — Complexity: S — Depends on: F1.4. *(Implemented: `src/components/Navbar.tsx` -- deliberately minimal (a title bar only); no user menu/notifications/branding since nothing downstream needs them yet (F3's auth, F13's alerts will add to this later, not replace it).)*
- [x] F5.2 Sidebar (nav links matching F2.1's routes). — Complexity: S — Depends on: F1.4. *(Implemented: `src/components/Sidebar.tsx`, generated from `routes.ts`'s `NAV_ROUTES` (`NavLink` active-state styling) so it can't drift from F2.1's route list.)*

### Milestone F3 — Authentication

Blocked on `F0.11 [BACKEND TODO]` for anything beyond a stubbed/mocked login — do not build a real credential store client-side.

- [x] F3.1 Login page + auth state store (token/session in memory + refresh strategy — finalize once F0.11 defines the mechanism). — Complexity: M — Depends on: F0.11. *(Implemented for mock-auth's scope: `src/store/authStore.ts`'s Zustand `useAuthStore` holds `session: {analyst, expiresAt} | null` in memory only (no persist middleware/localStorage -- a reload intentionally returns to `/login`), with `login(analyst)`/`logout()` actions and an `isSessionValid()` helper. `analyst` is a free-text identifier, not a verified credential -- mirrors the backend's existing `ApiStateWriter.get_or_create_user()` mock-auth convention (CLAUDE.md's F0 addendum), and there is deliberately no password field/credential store per this milestone's own instruction. `src/pages/LoginPage.tsx` is the form (name only) when `env.mockAuthEnabled` (F3.4); when disabled it shows an honest "auth unavailable" message rather than a fake login flow with nothing behind it. "Refresh strategy" is explicitly NOT finalized -- there is no real token to refresh until F0.11 defines one; `expiresAt` is a client-side-only TTL (12h) standing in for real expiry so F3.3 has something concrete to check, with no server-side enforcement behind it.)*
- [x] F3.2 Route guarding + redirect-to-login on 401 from the API client (F4.3). — Complexity: S — Depends on: F3.1, F4.3. *(Both halves now real: route guarding (`src/components/ProtectedRoute.tsx`, unchanged from when this was `[~]`) plus F4.3's `queryClient.ts` global error handler, which now logs out and redirects to `/login` on any `ApiError` with `status === 401`. Honest caveat carried forward from the partial note this replaces: no endpoint actually returns 401 today (F0.11's real auth is still undone), so this path is wired and tested but currently unexercised by the live app -- it activates the moment F0.11 makes 401 a real possible response.)*
- [x] F3.3 Logout + session-expiry handling. — Complexity: S — Depends on: F3.1. *(Implemented: `src/components/Navbar.tsx` shows the logged-in analyst's name + a "Log out" button (only when a session exists), calling `authStore.logout()` and navigating to `/login`. Session-expiry is `ProtectedRoute`'s `isSessionValid()` check against `expiresAt` -- an expired session is treated identically to no session (redirect to `/login`), the same client-side-TTL caveat noted in F3.1.)*
- [x] F3.4 Until F0.11 ships: build the entire app behind a mock-auth toggle (env-flag bypass) so F4-F15 aren't blocked waiting on backend auth design. — Complexity: S — Depends on: F1.3. *(Implemented: `VITE_MOCK_AUTH_ENABLED` (`src/config/env.ts`'s `env.mockAuthEnabled`, defaults to `true` when unset -- it's the only functional mode until F0.11 exists). `LoginPage` is the one place that branches on it; every other page is already gated uniformly by `ProtectedRoute`/the auth store regardless of the flag, so F4-F15 build against the same real session shape whether or not mock-auth is enabled.)*

### Milestone F4 — API Integration Layer

- [x] F4.1 Typed API client generated from F0.15's OpenAPI schema (or hand-written typed fetch wrappers if codegen is deferred). — Complexity: M — Depends on: F0.15. *(Decided: hand-written, not codegen — F0's surface is 9 small routers that change rarely, so a generated client would add a build-time dependency without buying much over an explicit, small file kept in sync by hand (the same tradeoff schemas.py itself already made relative to the dataclasses it mirrors). Implemented: `frontend/src/types/api.ts` (1:1 TS mirror of `schemas.py`), `frontend/src/api/client.ts` (`apiRequest()` — parses F0.15's `{"error": {code, message}}` envelope into a typed `ApiError`, plus a distinct `ApiNetworkError` for a `fetch` that never reached a server response), `frontend/src/api/endpoints.ts` (one typed function per backend route, grouped by router 1:1).)*
- [x] F4.2 TanStack Query hooks per endpoint (`useMetricsSnapshot`, `useEntityScores`, `useMotifCompletions`, `useEntityForensics`, `useAuditLog`, `useProtocolConfig`, `useMotifConfig`) with sensible `staleTime`/`refetchInterval` per data's real update cadence (e.g. metrics snapshot polls; forensics/config are near-static). — Complexity: M — Depends on: F4.1, F0.2-F0.9. *(Implemented under `frontend/src/hooks/api/`: the seven named here plus `useMotifResets`/`useMotifFeedback`+`useSubmitMotifFeedback`/`usePrunedEdge`/`useHealth`/`useAlertAck` to cover the rest of F0's surface. Cadence split three ways: live-ish data (metrics snapshot, entity scores, motif completions/resets, audit log) polls every 5-10s as a fallback until F13's live stream (F4.6) is mounted on a given page; near-static data (protocol/motif config) has a 5-minute `staleTime` and no polling, since both only change via a hand-edited YAML + `reload()`; point-in-time historical data (forensics) has a long/`Infinity` `staleTime` since a pruned edge's record never changes once written.)*
- [x] F4.3 Centralized error handling (toast on failure via F5.12, 401 → redirect) and loading-state conventions consumed by F5's skeletons. — Complexity: S — Depends on: F4.1. *(Implemented: `frontend/src/api/queryClient.ts`'s configured `QueryClient` — a `QueryCache`/`MutationCache` `onError` toasts (via F5.11's `toast`, not F5.12 as this line's own text says — F5.12 is the loading-skeleton components; that looks like this task's own typo) any failure, but only a query's *first* failure (one with no cached data yet) so a backgrounded polling query's transient miss doesn't interrupt the user every refetch interval. A 401 additionally calls `useAuthStore.getState().logout()` and `router.navigate(ROUTES.login)` — this is also the other half of F3.2, unblocking it (see that line below); no endpoint actually requires auth yet (F0.11 undone), so this path is wired but currently unexercised in practice. Loading-state convention: hooks expose TanStack Query's own `isLoading` (no data yet — pair with F5.12's skeletons) vs. `isFetching` (a background refetch with stale data still shown) as-is, rather than inventing a parallel convention.)*
- [x] F4.4 Retry logic (TanStack Query's built-in retry/backoff, tuned per endpoint — e.g. don't retry a 404 on a specific entity). — Complexity: S — Depends on: F4.2. *(Implemented as one shared `shouldRetry()` in `queryClient.ts` rather than per-endpoint overrides — the rule ("retry 5xx/network failures up to 3 attempts, never retry a 4xx") is the same for every endpoint including `usePrunedEdge`'s 404 case, so a single default naturally covers the example this task names without needing a per-hook exception. Mutations (`useSubmitMotifFeedback`, `useAlertAck`) explicitly opt out of retry entirely, since retrying a POST risks a duplicate submission. Uses TanStack Query's default exponential backoff (1s/2s/4s, capped 30s).)*
- [x] F4.5 Pagination hooks matching F0.15's chosen pagination envelope, reused by F9 (Detection Matrix) and F11 (Log Explorer). — Complexity: S — Depends on: F4.1, F0.15. *(Implemented: `frontend/src/hooks/api/pagination.ts`'s `toOffsetParams()`/`toPaginatedResult()`, used by every paginated hook in F4.2. Handles the one real wrinkle in F0.15's envelope: `total` is populated for `audit.log` (a full file scan per request) but `null` for scores/motif completions/resets/feedback (no `COUNT(*)` query behind those) — `toPaginatedResult()` computes an exact `pageCount` when `total` is known, else a running floor (current page, +1 once a full page proves another might exist) so F5.4's `DataTable` still gets a real number for its manual-pagination `pageCount` prop either way.)*
- [x] F4.6 WebSocket/SSE client wrapper (connect, reconnect-with-backoff, message dispatch into TanStack Query cache updates or a dedicated live-event store) for F0.10, consumed by F13. — Complexity: L — Depends on: F0.10. *(Implemented: `frontend/src/api/liveStream.ts`'s `LiveStreamManager` + `useLiveStream()` hook, `frontend/src/store/liveStreamStore.ts`'s Zustand store — both halves this task names, not just one. Deliberately does not rely on native `EventSource` auto-reconnect (a fixed ~3s retry with no backoff and no observable status) — every transport-level `onerror` closes the connection and schedules the class's own reconnect with exponential backoff (1s → 30s cap, reset on the next clean `onopen`). Each named SSE event (`motif_completion`/`motif_reset`/`inference_result`/`prune`) both pushes into the live-event store (bounded to the 200 most recent, for F13.2's raw feed) and invalidates the matching F4.2 query key, so any already-mounted list picks up the change via a normal refetch rather than a hand-spliced cache update. `heartbeat` updates a timestamp with no store-feed entry; the server's own named `error` event (a Postgres hiccup mid-poll, per stream.py) is recorded separately from a transport failure since the connection itself is still alive when it fires. Not mounted anywhere yet — F13 hasn't landed — same "installed, not yet wired into a page" posture F1.4 already established for Recharts/Zustand; `eventSourceFactory` is an injectable seam so tests don't need a real `EventSource` (absent in jsdom).)*

### Milestone F5 — Reusable UI Component Library

- [x] F5.1 Navbar. — Complexity: S — Depends on: F1.4. *(Pulled forward into Milestone F2's pass -- see F2.2's note above. `src/components/Navbar.tsx`.)*
- [x] F5.2 Sidebar (nav links matching F2.1's routes). — Complexity: S — Depends on: F1.4. *(Pulled forward into Milestone F2's pass -- see F2.2's note above. `src/components/Sidebar.tsx`.)*
- [x] F5.3 Dashboard card / stat-card components (used by F6, F14). — Complexity: S — Depends on: F1.4. *(Found already implemented in the working tree while landing F4 -- not authored in this pass, just verified and flipped here since tasks.md hadn't caught up. `frontend/src/components/stat-card.tsx`: normal/`loading` (F4's fetch-in-flight convention)/`unavailable` (an inline F5.13-style stand-in, e.g. for F6.1's pending F0.12 tile) states built in.)*
- [x] F5.4 Data table component with sort/pagination/row-selection (used by F9, F10, F11). — Complexity: M — Depends on: F1.4, F4.5. *(Found already implemented -- see F5.3's note. `frontend/src/components/data-table.tsx`, built on `@tanstack/react-table`: works fully client-side with no props beyond `columns`/`data`, or accepts `pageCount`/`pagination`/`onPaginationChange` for F4.5's server-driven pagination -- F4.5's hooks (this pass) now supply exactly that shape.)*
- [x] F5.5 Chart wrapper components around the F1.4 charting library (line/time-series, bar, pie/donut, heatmap) — used by F12. — Complexity: M — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/charts.tsx`, built on Recharts.)*
- [x] F5.6 Filter bar / filter-chip components (used by F8, F9, F11). — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/filter-bar.tsx`.)*
- [x] F5.7 Search bar (used by F11, F10's user list). — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/search-bar.tsx`.)*
- [x] F5.8 Date-range picker supporting the F8 presets (last hour/24h/7d/30d) + custom range. — Complexity: M — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/date-range-picker.tsx`, built on `react-day-picker`.)*
- [x] F5.9 Modal / dialog primitive. — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. The shadcn `Dialog` primitive (`frontend/src/components/ui/dialog.tsx`) plus `frontend/src/components/confirm-dialog.tsx`, a composed confirm-before-destructive-action convenience on top of it.)*
- [x] F5.10 Alert banner (severity-colored, used by F13's critical alerts). — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/alert-banner.tsx`, sharing severity tokens with F5.14's badges.)*
- [x] F5.11 Toast notification system. — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/toast.ts` re-exports `sonner`'s `toast`; its `Toaster` is mounted once in `main.tsx`. F4.3's global error handler is this pass's first real caller.)*
- [x] F5.12 Loading skeletons (per component consuming F4's loading states). — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/skeletons.tsx` covers charts/simple lists -- F5.3/F5.4 each already render their own matching-layout skeleton internally via a `loading` prop, so no separate component was needed for those two.)*
- [x] F5.13 Empty-state components (no data / no results / feature-pending-backend states — used wherever an F0 `[BACKEND TODO]` isn't done yet, so the UI degrades honestly instead of showing fabricated data). — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/empty-state.tsx`: generic `EmptyState`, `NoResultsState`, and `BackendPendingState` (names the specific `tasks.md` task ref, e.g. "F0.12", rather than a generic message).)*
- [x] F5.14 Severity/status badges (threat severity, investigation status, false-positive marker — shared vocabulary across F9/F10/F11). — Complexity: S — Depends on: F1.4. *(Found already implemented -- see F5.3's note. `frontend/src/components/severity-badge.tsx`: `SeverityBadge`/`InvestigationStatusBadge`/`DispositionBadge`.)*

### Milestone F6 — Executive Dashboard

- [x] F6.1 Company cybersecurity score tile. *(Depends on `[BACKEND TODO]` F0.12 — build behind F5.13's empty-state until that ships.)* — Complexity: S — Depends on: F0.12, F5.3. *(Implemented: `frontend/src/features/dashboard/CybersecurityScoreTile.tsx` — no hook, since there's nothing to fetch yet; renders F5.3's `StatCard.unavailable` naming the exact gap ("depends on tasks.md F0.12") rather than a generic placeholder, per F5.13's own empty-state convention.)*
- [x] F6.2 Overall security level indicator, derived from the same F0.12 aggregate or a simpler interim proxy (e.g. thresholded `MetricsSnapshot.motif_hit_rate_per_second` + latest max entity score) — document which if used as an interim. — Complexity: S — Depends on: F4.2. *(Decided: the interim proxy, exactly as this line's own example — `frontend/src/features/dashboard/logic.ts`'s `computeSecurityLevel()` takes the worse of two thresholded signals: `motif_hit_rate_per_second` (real, FR3.4) and `abs()` of the single highest-magnitude entity score (`useEntityScores` with `pageSize: 1`, relying on F0.3's `ORDER BY abs(score) DESC`). Thresholds are documented in-code as illustrative constants, not a calibrated formula, to be revisited once F0.12 exists. `SecurityLevelTile.tsx` renders the result via a new `StatusPill` (this folder) rather than F5.14's `SeverityBadge`, since "security level" isn't the same vocabulary as F5.14's threat-detection severity.)*
- [x] F6.3 Threat status indicator (real-time-ish, from `MetricsSnapshot` + recent `MotifCompletionEvent` count). — Complexity: S — Depends on: F4.2. *(Implemented: `logic.ts`'s `computeThreatStatus()` counts `useMotifCompletions`' most recent page within a 15-minute window (`THREAT_STATUS_WINDOW_SECONDS`) — quiet/active/critical thresholded on that count. `ThreatStatusTile.tsx` anchors "now" to the query's own `dataUpdatedAt` rather than a live `Date.now()` read during render, keeping the component pure (the React Compiler's purity lint otherwise flags a direct `Date.now()` call in render).)*
- [x] F6.4 System health tile — process/pipeline liveness. *(Backing note: today "health" would mean "is `scripts/run_pipeline.py` running," which F0.1's process-model decision must expose, e.g. a `/api/health` endpoint hitting Redis/Neo4j connectivity + last-metrics-snapshot age.)* — Complexity: S — Depends on: F0.1. *(Implemented: `SystemHealthTile.tsx` via F4.2's `useHealth()` — overall ok/degraded plus a per-dependency Postgres/Neo4j/Redis breakdown (`StatusPill`/`DependencyDot`, this folder).)*
- [x] F6.5 Active monitoring status (derived the same way as F6.4). — Complexity: S — Depends on: F6.4. *(Implemented: `MonitoringStatusTile.tsx` + `logic.ts`'s `computeMonitoringStatus()`, also reading `/api/health` but a different field than F6.4 — `last_metrics_snapshot_age_seconds`, i.e. whether the pipeline process itself still appears to be writing, not whether the infra it writes to is merely reachable. Active/stale/inactive thresholds (30s/300s) are provisional, same caveat as F6.2.)*
- [x] F6.6 Last analysis timestamp (`MetricsSnapshot`/latest `InferenceResult.t`). — Complexity: S — Depends on: F4.2. *(Decided: neither literal option was directly usable — `MetricsSnapshotOut` carries no timestamp field at all, and `InferenceResult.t` is only available per-entity-sorted-by-score, not by time. Used `/api/health`'s `last_metrics_snapshot_age_seconds` instead (`LastAnalysisTile.tsx`) — "now minus that age," anchored to the health query's own `dataUpdatedAt` rather than a live `Date.now()` (same purity reasoning as F6.3) — rendered as a relative time via `date-fns`' `formatDistanceToNowStrict`.)*
- [x] F6.7 Assemble dashboard grid layout from F6.1-F6.6 + F5.3 cards. — Complexity: M — Depends on: F6.1-F6.6. *(Implemented: `frontend/src/pages/HomePage.tsx` — a responsive CSS grid of all six tiles. Each tile fetches its own data independently (TanStack Query's usual co-location pattern) rather than the page threading props down, so one slow/failed tile's query never blocks the others from rendering. Incidental fix made alongside this task: added `HydrateFallback` elements to `router.tsx`'s top-level routes (`src/components/RouteHydrateFallback.tsx`) — without one, a data router with `lazy`-loaded routes (F2.3) renders nothing at all until that page's chunk resolves, which surfaced as an intermittent `router.test.tsx` timeout once `HomePage`'s chunk grew heavier; also raised the test suite's shared `findBy`/`waitFor` timeout (`src/test/setup.ts`) for the same reason.)*

### Milestone F7 — Threat Analytics

- [x] F7.1 Malicious / suspicious / benign user counts. *(Backing gap: there is no persisted per-user classification today — `InferenceResult.score` is a magnitude with no fixed sign meaning (specs.md §4 explicitly leaves the untrained reference model's output uninterpreted beyond relative magnitude) and no stored threshold buckets these into 3 tiers. Needs a `[BACKEND TODO]`-style decision: either bucket by score threshold (document the thresholds chosen, and that they're provisional given the model is untrained per CLAUDE.md's Phase 5 notes) or wait for a trained/calibrated model. Do not present these buckets as ground truth in the UI copy.)* — Complexity: M — Depends on: F4.2. *(Decided: bucket by score threshold, reusing F6.2's exact `maxAbsScore` interim-proxy constants (`elevated`/`critical`) under new names (`suspicious`/`malicious`) rather than a second set of unreviewed magic numbers — `frontend/src/features/analytics/logic.ts`'s `classifyEntityScore()`/`countUserThreatTiers()`, restricted to `User:*` entity ids per this line's own "user counts" wording. Sampled from `useEntityScores`'s top-500-by-`|score|` page (F0.3's `scores.py` caps `limit` at 500) — not literally every user in the deployment; a true population count needs a dedicated aggregate, the same F0.12 gap this line already names. `UserThreatCountsPanel.tsx` renders the three counts via `status-pill.tsx`'s tone vocabulary plus an explicit "Provisional score-threshold buckets ... not a calibrated classification" caption, satisfying this line's "do not present as ground truth" instruction as load-bearing UI copy, not decoration.)*
- [x] F7.2 Threat trends over time (time-series of motif completions + high-|score| entities, via F5.5 line chart). — Complexity: M — Depends on: F4.2, F5.5. *(Implemented: `logic.ts`'s `buildThreatTrendSeries()` buckets the last 24h into hourly buckets (F8.1's real time-range control doesn't exist yet, so this window is fixed/provisional, same spirit as F6.3's window) — `attacks` from motif completions' `completed_at`, `highRiskEntities` from non-benign entity scores' `t`. Documented in-code as an honest snapshot-in-time proxy, not a true history: `entity_scores` is upserted/latest-value-only (CLAUDE.md's F0 notes), so an entity's high-risk status only ever shows up in whichever single bucket its most recent inference happened to land in. `ThreatTrendsChart.tsx` renders it via F5.5's `TimeSeriesChart`.)*
- [x] F7.3 Threat severity distribution (pie/bar via F5.5, bucketed the same way as F7.1 — same threshold-provisionality caveat). — Complexity: S — Depends on: F7.1, F5.5. *(Implemented: `logic.ts`'s `buildSeverityDistribution()` reuses F7.1's exact tier classification but tallies every entity in the sample (Users and Machines alike), not just users. `ThreatSeverityChart.tsx` renders it via F5.5's `DonutChart`, colored with the `--status-success/warning/error` tokens (this genuinely is severity data, so it earns those tokens rather than `charts.tsx`'s default categorical `--chart-1..5` palette).)*
- [x] F7.4 Live attack counter (count of `MotifCompletionEvent`s in a rolling window, via F4.6's live stream). — Complexity: S — Depends on: F4.6. *(Implemented: `LiveAttackCounter.tsx` is F4.6's first real mount (`useLiveStream()`), ahead of Milestone F13's broader "wire every F6/F7/F9 tile up to the live stream" pass (F13.1) — this line names F4.6 as a direct dependency, not a polling fallback, so it doesn't wait for F13. `logic.ts`'s `computeLiveAttackCount()` counts `motif_completion` events in `useLiveStreamStore`'s event feed within a 5-minute rolling window, anchored to the stream's own `lastHeartbeatAt` rather than a live `Date.now()` read during render (same purity reasoning as F6.3/F6.6) — until the first heartbeat arrives, the tile shows its loading/unavailable state instead of guessing. Landing this task also added a no-op `EventSource` stub to `frontend/src/test/setup.ts` (jsdom has none) so tests that mount this tile via a real page/route, rather than mocking `@/api/liveStream` outright, don't crash — and bumped Vitest's `testTimeout` (`vite.config.ts`) above `setup.ts`'s `asyncUtilTimeout`, fixing a latent race between the two that F6.7's own router-flake fix didn't need to touch but this milestone's added test files made visible.)*

All four tiles are assembled onto `frontend/src/pages/AnalyticsPage.tsx` (previously a placeholder pointing at Milestone F12) under a "Threat Analytics" heading; Milestones F8 (time-range filtering) and F12 (the rest of this page's charts) build further onto this same page/layout.

### Milestone F8 — Time-Based Analytics

- [ ] F8.1 Time-range filter control (last hour / 24h / 7d / 30d / custom) using F5.8, applied as query params across F7/F9/F11's data hooks. — Complexity: M — Depends on: F5.8, F4.2.
- [ ] F8.2 "Number of hackers detected" metric for the selected range. *(Same F7.1 classification caveat applies — this is a count of entities crossing whatever threshold F7.1 settles on, not an authoritative label.)* — Complexity: S — Depends on: F8.1, F7.1.
- [ ] F8.3 "Number of attacks" metric (motif completions in range). — Complexity: S — Depends on: F8.1.
- [ ] F8.4 Threat rate (attacks / time) and detection rate metrics — detection rate should be sourced from `src/t_gnn/pilot.py`'s real `precision`/`recall` figures where a pilot report exists (`pilot-report.json`'s shape), not invented; clearly label it as "last pilot evaluation," not live, since `pilot.py` is a batch tool, not a continuous one. — Complexity: M — Depends on: F0.9 (or a new `/api/pilot/latest-report` endpoint — add to F0 if pilot reports should be API-served rather than file-only). — 
- [ ] F8.5 Average anomalies per hour (derived from `InferenceResult` volume/`DeviationSignal` counts over the selected range). — Complexity: S — Depends on: F8.1.

### Milestone F9 — Detection Matrix

- [ ] F9.1 Table columns: severity, confidence score, detection category, detection model, timestamp, source, status, false-positive marker, investigation status — using F5.4/F5.14. — Complexity: M — Depends on: F5.4, F5.14.
- [ ] F9.2 Populate rows from `MotifCompletionEvent` (F0.4) — maps directly: `confidence` → confidence score, `motif_name` → detection category, `completed_at` → timestamp, `chain_key` → source. — Complexity: M — Depends on: F0.4, F9.1.
- [ ] F9.3 Populate rows from anomaly-path detections (`InferenceResult` where `trigger == "scheduled"`/high `|score|`) so the matrix covers both detection paths (motif + T-GNN deviation), matching `pilot.py`'s own two-path evaluation split. — Complexity: M — Depends on: F0.3, F9.1.
- [ ] F9.4 "Detection model" column values: `motif:<motif_name>` vs. `tgnn_deviation` — there is no other named model in this repo; don't invent additional model names. — Complexity: S — Depends on: F9.2, F9.3.
- [~] F9.5 False-positive marker + investigation status columns, writable via `src/t_gnn/feedback.py`'s `MotifFeedbackBus`/analyst disposition (`true_positive`/`false_positive`) — this is real backend logic already, just needs an API surface (`POST /api/motifs/{completion_id}/feedback` — add to F0). *(API side done: `POST`/`GET /api/motifs/feedback` (`src/t_gnn/api/routers/motifs.py`), persisting to the new `motif_feedback` table. `analyst` is accepted as free-text for now (F0.11's mock-auth identity), not a verified session. Frontend consumption of this endpoint in the Detection Matrix table itself is still not built.)* — Complexity: M — Depends on: F0.11 (needs an `analyst` identity to attribute the disposition to, per `MotifFeedbackEvent.analyst`).
- [ ] F9.6 Filtering (by severity/status/category) and sorting. — Complexity: M — Depends on: F9.1, F5.6.

### Milestone F10 — User Investigation

- [ ] F10.1 User list page (all `User:*` entities seen — needs a new `/api/entities?type=User` listing endpoint; add to F0, backed by `ActiveGraphStore`'s known node ids or a Neo4j distinct-entity query). — Complexity: M — Depends on: F0.1.
- [ ] F10.2 User detail page shell + routing (`/investigation/:entityId`). — Complexity: S — Depends on: F2.1, F10.1.
- [ ] F10.3 Risk score display — reuses F0.3's `InferenceResult.score` for that entity; same provisional-interpretation caveat as F7.1. — Complexity: S — Depends on: F0.3.
- [ ] F10.4 Timeline of activity — `src/t_gnn/forensics.py`'s `reconstruct_activity()` via F0.6; this is real, already-designed-for-this-exact-purpose data (design.md §2.7's "reconstruct activity in a time window"). — Complexity: M — Depends on: F0.6.
- [ ] F10.5 Triggered rules — motif completions/deviation signals where this entity is the `chain_key`/`edge.src`, from F0.4/F0.3. — Complexity: M — Depends on: F0.3, F0.4.
- [ ] F10.6 Log history — same data as F10.4 (there is no separate raw-log store per entity beyond pruned-edge cold storage) — don't build a second, different-looking "log history" panel that implies a different data source than the timeline. — Complexity: S — Depends on: F10.4.
- [ ] F10.7 `[BACKEND TODO]` IP addresses panel — blocked on F0.13; build behind F5.13's empty-state until/unless that schema gap is resolved. — Complexity: S — Depends on: F0.13.
- [ ] F10.8 `[BACKEND TODO]` Devices panel — same blocker as F10.7 (no device field in the schema). — Complexity: S — Depends on: F0.13.
- [ ] F10.9 `[BACKEND TODO]` Session history panel — no session concept exists in `Edge`/`PrunedEdgeRecord`; would need to be derived (e.g. contiguous-activity windowing over F10.4's timeline) or backed by a new field — decide which before building; don't fabricate session boundaries silently. — Complexity: M — Depends on: F0.13.

### Milestone F11 — Log Explorer

- [ ] F11.1 Search logs — full-text/field search over F0.8's audit-log records (prune/motif-reset events); clarify in UI copy that this is the *audit trail*, not raw ingested Sysmon/Windows events (no raw-event store exists — `raw_event_id` is carried through `Edge`/`PrunedEdgeRecord` as an opaque reference back to a source system this repo doesn't itself store). — Complexity: M — Depends on: F0.8.
- [ ] F11.2 Filter logs (by type/time-range/entity) using F5.6/F8.1. — Complexity: M — Depends on: F11.1, F5.6.
- [ ] F11.3 View raw log entry (the full NDJSON record F0.8 returns, pretty-printed). — Complexity: S — Depends on: F11.1.
- [ ] F11.4 Highlight malicious events (motif-reset / high-severity prune records styled via F5.14). — Complexity: S — Depends on: F11.1, F5.14.
- [ ] F11.5 Export filtered logs (CSV/JSON download of the current filtered view). — Complexity: S — Depends on: F11.2.
- [ ] F11.6 Pagination via F4.5/F5.4. — Complexity: S — Depends on: F4.5, F5.4.
- [ ] F11.7 Live updates via F4.6's stream, appended to the top of the table with clear "new" affordance rather than silently reordering under the user. — Complexity: M — Depends on: F4.6, F11.1.

### Milestone F12 — Analytics Visualizations

- [ ] F12.1 Threat timeline chart. — Complexity: S — Depends on: F5.5, F7.2.
- [ ] F12.2 Attacks-per-day chart. — Complexity: S — Depends on: F5.5, F8.3.
- [ ] F12.3 Detection accuracy chart — sourced from `pilot.py`'s real precision/recall (F8.4's endpoint), explicitly labeled as "as of last pilot run" rather than live, matching what the data actually represents. — Complexity: S — Depends on: F8.4.
- [ ] F12.4 Severity pie chart — same F7.1 threshold caveat. — Complexity: S — Depends on: F7.3.
- [ ] F12.5 `[BACKEND TODO]` Geographic attack map — blocked on F0.14; descope or stub with an explicit "no location data available" state (F5.13) rather than a placeholder map with fake pins. — Complexity: S (stub) / L (real, pending F0.14) — Depends on: F0.14.
- [ ] F12.6 Heatmap of attack frequency (time-of-day × day-of-week over motif completions/anomalies — real data, no backend gap). — Complexity: M — Depends on: F5.5, F0.4.
- [ ] F12.7 Top targeted resources chart (most-frequently-`dst`-appearing `Machine:*` entities in recent detections). — Complexity: M — Depends on: F0.4, F0.3.
- [ ] F12.8 Most common attack patterns chart (motif-completion counts grouped by `motif_name`, i.e. `config/motifs.yaml`'s library — currently just `admin_share_escalation`/`lateral_pivot`; chart should scale automatically as the motif library grows per tasks.md's Open Questions item on the full motif library). — Complexity: S — Depends on: F0.4.

### Milestone F13 — Live Monitoring

- [ ] F13.1 Real-time updates wiring via F4.6 across F6/F7/F9's live tiles. — Complexity: M — Depends on: F4.6.
- [ ] F13.2 Live event stream panel (raw feed of incoming `MotifCompletionEvent`/`PrunedEdgeEvent`/`InferenceResult`). — Complexity: M — Depends on: F4.6.
- [ ] F13.3 Auto-refresh toggle + interval control (client-side polling fallback for endpoints not on the F0.10 stream, e.g. F0.2's metrics snapshot). — Complexity: S — Depends on: F4.2.
- [ ] F13.4 Notification panel (recent alerts, unread count). — Complexity: M — Depends on: F13.2, F5.11.
- [ ] F13.5 Critical alerts (severity-filtered subset of F13.2, using F5.10's banner). — Complexity: S — Depends on: F13.2, F5.10.
- [~] F13.6 Alert acknowledgement — persists an ack state per detection. *(Backend gap: no ack/disposition-beyond-feedback field exists on completions today; extend F9.5's feedback endpoint or add a dedicated `POST /api/alerts/{id}/ack` to F0 rather than faking it client-side-only, since an ack that resets on page reload isn't useful for a SOC workflow.)* *(API side done: `POST /api/alerts/ack` (`src/t_gnn/api/routers/alerts.py`), keyed on `detection_type`+`detection_ref` rather than a single unified alert id, since motif completions and anomaly-path detections don't share one id space today — persisted to the new `alert_acknowledgements` table. Frontend consumption (the actual ack button/UI state in Live Monitoring) is still not built.)* — Complexity: M — Depends on: F0.11.

### Milestone F14 — Company Security Overview

Largely a second view over F6/F0.12's data at a different altitude (company-wide rollup vs. per-tile) — reuse F6's hooks rather than duplicating fetch logic.

- [ ] F14.1 Overall security posture + threat level (reuse F6.1/F6.2). — Complexity: S — Depends on: F6.1, F6.2.
- [ ] F14.2 Detection engine status + response status (reuse F6.4/F6.5; "response status" has no backend concept today beyond "is the pipeline running" — don't imply an automated-response/SOAR capability, which specs.md §4 explicitly excludes as a non-goal). — Complexity: S — Depends on: F6.4.
- [ ] F14.3 Number of monitored users / analyzed sessions / total processed logs. *(Backend gap: "sessions" isn't a modeled concept (see F10.9); "monitored users" can come from F10.1's entity listing; "processed logs" can come from a cumulative counter — check whether `MetricsCollector` needs a new lifetime-counter field, since `RollingRateCounter` today is rate-only, not cumulative — add to F0 if so.)* — Complexity: M — Depends on: F10.1, F0.2.
- [ ] F14.4 Average response time — no such timing is currently measured anywhere (inference latency is measured; "response" in the SOAR/analyst-action sense is not). Define what this measures (time-to-detection from `t_e` to `completed_at`/`t`? analyst ack latency from F13.6?) before implementing — likely a new `[BACKEND TODO]`. — Complexity: M — Depends on: F13.6.

### Milestone F15 — Settings

- [ ] F15.1 Theme setting (ties into F16.2's dark mode). — Complexity: S — Depends on: F1.4.
- [ ] F15.2 Notification settings (which alert types/severities trigger F13.4's panel/toasts). — Complexity: S — Depends on: F13.4.
- [ ] F15.3 API configuration (base URL / environment — mostly relevant for local dev / pointing at a different backend instance). — Complexity: S — Depends on: F1.3.
- [ ] F15.4 Refresh interval setting, feeding F13.3. — Complexity: S — Depends on: F13.3.
- [ ] F15.5 Alert thresholds — read-only display of `config/protocols.yaml`/`config/motifs.yaml` via F0.9 to start; a write-path (edit λ_p / motif window from the UI) is a separate, larger `[BACKEND TODO]` since today those are hand-edited YAML + `ProtocolDecayRegistry.reload()`/`MotifRegistry.reload()` (see `docs/operational-runbook.md`) — scope read-only first, flag the write-path as a follow-up milestone rather than building it speculatively. — Complexity: M (read-only) / L (editable) — Depends on: F0.9.
- [ ] F15.6 User preferences (persisted client-side, e.g. localStorage, until F0.11's auth/user model exists to persist server-side). — Complexity: S — Depends on: F1.4.

### Milestone F16 — Quality, Accessibility & Testing

- [ ] F16.1 Responsive design pass across all pages (mobile/tablet breakpoints for the dashboard grid, tables, charts). — Complexity: M — Depends on: F6-F14 substantially complete.
- [ ] F16.2 Dark mode (design-token-driven, per F15.1). — Complexity: M — Depends on: F1.4.
- [ ] F16.3 Accessibility pass (keyboard nav, ARIA labels on charts/tables/alerts, color-contrast check on severity badges (F5.14) since severity coloring can't be the only signal). — Complexity: M — Depends on: F5 components complete.
- [ ] F16.4 Unit tests (components, hooks) — establish the frontend's test runner/convention now (nothing exists yet; don't retrofit at the end). — Complexity: M — Depends on: F1.1.
- [ ] F16.5 Integration tests (page-level, mocking F4's API layer). — Complexity: M — Depends on: F16.4.
- [ ] F16.6 Performance optimization pass (bundle size, memoization on high-frequency live-updating components from F13). — Complexity: M — Depends on: F13 complete.
- [ ] F16.7 Code splitting verification (confirm F2.3's route-level splitting is actually reducing initial bundle). — Complexity: S — Depends on: F2.3.
- [ ] F16.8 Lazy loading for below-the-fold/heavy components (charts, tables with large datasets). — Complexity: S — Depends on: F5.4, F5.5.
- [ ] F16.9 Error boundaries per major page/section so one panel's failure (e.g. a `[BACKEND TODO]` endpoint that 404s) doesn't blank the whole dashboard — pairs with F5.13's empty states. — Complexity: S — Depends on: F2.5, F5.13.

### Milestone F17 — Deployment

- [ ] F17.1 Production build pipeline (Vite build, env-specific config injection). — Complexity: S — Depends on: F1.3.
- [ ] F17.2 Containerize the frontend (Dockerfile) and add it as a service in `docker-compose.yml` alongside Flink/Redis/Neo4j, so `docker compose up -d` can optionally bring up the dashboard too. — Complexity: M — Depends on: F17.1, F0.1 (the API service also needs a compose entry).
- [ ] F17.3 Reverse-proxy/CORS configuration between the frontend origin and F0's API. — Complexity: S — Depends on: F17.2.
- [ ] F17.4 CI deployment job (build + push image / static deploy, per F1.7's CI). — Complexity: M — Depends on: F1.7, F17.2.
- [ ] F17.5 Update `README.md` and `docs/cli-reference.md`-adjacent docs (a new `docs/frontend-reference.md`?) with dashboard setup/run instructions once F1-F2 land, per this repo's existing convention of keeping non-planning docs current with what's implemented (see `CLAUDE.md`'s "End-of-phase checklist" — the same discipline applies here even though this isn't a numbered backend phase). — Complexity: S — Depends on: F17.1.

## Open Questions (tracked, not yet actionable)

- [ ] Determine full initial motif library beyond the seed example (see specs.md §8).
- [ ] Decide whether `ε` should be per-protocol vs. global (see specs.md §8).
- [ ] Evaluate seasonality-awareness need for baseline deviation model (see design.md §6).
- [ ] What is the SLA for cold-storage query latency during forensic investigations? (see specs.md §8 — not previously tracked in this file.)
