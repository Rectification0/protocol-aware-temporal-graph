# Local Postgres dev database (used instead of standing up docker-compose.yml
# for now -- see README.md "Local dev database" section).
# Copy this file to .env and fill in your local credentials -- do not commit .env.
PGHOST=localhost
PGPORT=5432
PGDATABASE=t_gnn_dev
PGUSER=postgres
PGPASSWORD=changeme
